import { useQuery } from '@tanstack/react-query'

export default function Dashboard() {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>This is a temp-worker management app</p>
    </div>
  );
}
